# max-shared — shared core for MAX Bot API clients.
#
# Shared models, API client, message converter, and utilities
# used by both max-hermes (standalone bridge) and max-hermes-plugin (Hermes gateway plugin).

__version__ = "1.0.0"
